/* 

Temos o array [15, 8, 10, 25, 12, 30, 5, 20, 18, 7] e precisamos criar um código em JavaScript para encontrar o valor 20.

Para isso, temos que escolher entre realizar uma busca linear ou binária de acordo com a que for mais adequada para essa situação.

Codifique a solução mais eficiente para buscar o número 20 no array.

*/

// Nesse caso utilizaremos a busca binária pois os elementos não estão ordenados.

let array = [15, 8, 10, 25, 12, 30, 5, 20, 18, 7];

let contador = 1;
for (let i = 0; i < array.length; i++) {
    console.log("Tentativa " +contador++);
    if (array[i] == 20) {
        console.log("Achei o número " +array[i]);
        break;
    } else {
        console.log(array[i]+ " não é o número que estou procurando.");
    }
}
